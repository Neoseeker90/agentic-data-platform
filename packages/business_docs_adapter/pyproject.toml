[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "business_docs_adapter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "contracts",
  "pydantic>=2.7.0",
  "asyncpg>=0.29.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/business_docs_adapter"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
