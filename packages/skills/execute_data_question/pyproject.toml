[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "execute_data_question"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "contracts",
  "skill_sdk",
  "lightdash_adapter",
]

[tool.hatch.build.targets.wheel]
packages = ["src/execute_data_question"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
